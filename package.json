{
  "name": "@plasticine-yang/eslint-config-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@7.1.0",
  "author": "Plasticine-Yang",
  "license": "MIT",
  "scripts": {
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "devDependencies": {
    "bumpp": "^8.2.1",
    "eslint": "^8.30.0"
  }
}
